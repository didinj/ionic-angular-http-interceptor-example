import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'http-interceptor-example',
  webDir: 'www'
};

export default config;
